FROM node:20-slim

WORKDIR /app
COPY package.json package-lock.json vite.config.js index.html ./
COPY public/ ./public/
COPY docs/ ./docs/

RUN npm ci

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]